name: Deploy UI Helpers

on:
  push:
    tags:
      - "ui-helpers-v*"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm install

      - name: Build UI Helpers
        run: npm run build --workspace=apps/mfe-ui

      - name: Install Vercel CLI
        run: npm i -g vercel

      - name: Deploy UI Helpers
        run: vercel deploy --prod --yes --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_UI_HELPERS_PROJECT_ID }}